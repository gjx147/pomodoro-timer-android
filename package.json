{
  "name": "pomodoro-timer-android",
  "version": "1.0.1",
  "description": "番茄钟 Android 应用",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static web, no build needed'",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "apk:debug": "cd android && ./gradlew assembleDebug"
  },
  "author": "gjx14",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
